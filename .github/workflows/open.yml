name: Open URL Locally

on:
  workflow_dispatch:

jobs:
  open-url:
    runs-on: self-hosted
    steps:
      - name: Open URL in Chrome
        shell: powershell
        run: |
          start "" "C:\Program Files\Google\Chrome\Application\chrome.exe" "https://digitaltools.wm.com"
          timeout /t 10
